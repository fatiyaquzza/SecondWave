<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> SecondWave || Sign Up </title>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body class="bg-black font-primary">
  @if (flashMessages.get('error'))
  <div id="alert" class="alert bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4">
    <div>{{ flashMessages.get('error') }}</div>
    <button type="button" class="absolute top-0 right-0 mt-1 mr-2 p-2 text-lg" onclick="closeAlert()">&times;</button>
  </div>
@endif
  <section class="flex w-full items-center justify-center mx-auto pl-24">
    <div class="w-1/2 mt-16">
      <div class="flex justify-center items-center my-auto">
        <img src="{{asset('/resources/images/signin2.png')}}" class="w-[600px]" />
      </div>
      <div class="mt-6">
        <p class="text-white text-center text-lg">
          if you already have an account <br>
          you can <a href="{{route('auth.login.show')}}" class="text-[#49A3AB]"> Sign in here! </a>
        </p>
      </div>
    </div>

    <div class="mt-16 text-white text-5xl w-1/2">

      <div>
        <p class="font-semibold">
          Sign Up to
        </p>
        <p class="font-light mt-4">
          Secondwave
        </p>
      </div>

      <form method="POST" action="{{ route('auth.register.store') }}" class="max-w-md mt-10">
        {{ csrfField() }}

        <div class="mb-5">
          <label for="fullName"></label>
          @inputError('fullName')
          {{ $messages.join(', ') }}
          @end
          <input type="text" id="fullName" name="fullName" value="{{old('username') || ''}}"
            class="bg-[#EAF0F7] text-gray-900 text-sm rounded-lg block w-full p-4 " placeholder="Enter Username"
            required />
        </div>

        <div class="mb-5">
          <label for="email"></label>
          @inputError('email')
          {{ $messages.join(', ') }}
          @end
          <input type="email" id="email" name="email" value="{{old('email') || ''}}"
            class="bg-[#EAF0F7] text-gray-900 text-sm rounded-lg block w-full p-4 " placeholder="Enter Email"
            required />
        </div>

        <div class="mb-5">
          <label for="role"></label>
          <select id="role" name="role" value="{{old('role') || ''}}"
            class="bg-[#EAF0F7] text-gray-900 text-sm rounded-lg block w-full p-4" required>
            <option selected>Select Role</option>
            <option>Buyer</option>
            <option>Seller</option>
          </select>
        </div>

        <div class="mb-5">
          <label for="password"></label>
          @inputError('password')
          {{ $messages.join(', ') }}
          @end
          <input type="password" id="password" name="password" value="{{old('password') || ''}}"
            class="bg-[#EAF0F7] text-gray-900 text-sm rounded-lg block w-full p-4 " placeholder="Enter Password"
            required />
        </div>

        <div class="mb-5">
          <label for="address"></label>
          <textarea id="alamat" name="address" value="{{old('alamat') || ''}}" rows="4"
            class="block p-3 w-full text-sm text-gray-900 bg-[#EAF0F7] rounded-lg border border-gray-300 "
            placeholder="Full Address"></textarea>
        </div>

        <div class="mb-5">
          <button type="submit"
            class="bg-[#49A3AB] text-lg rounded-lg block w-full p-4 text-white font-medium hover:bg-[#21575B]">Sign
            Up</button>
        </div>
      </form>
    </div>
  </section>

    <script>
    function closeAlert() {
      document.getElementById('alert').style.display = 'none';
    }
  </script>
</body>

</html>